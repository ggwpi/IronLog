const ACTIVE_KEY='ironlog:active-workout',REST_KEY_PREFIX='ironlog:rest:';let elapsedInterval=null,restInterval=null,saveTimer=null;function formatElapsed(ms){const t=Math.max(0,Math.floor(ms/1000)),h=Math.floor(t/3600),m=Math.floor(t%3600/60),s=t%60;return`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`}function restKey(id){return`${REST_KEY_PREFIX}${id}`}function stopTimers(){if(elapsedInterval)clearInterval(elapsedInterval);if(restInterval)clearInterval(restInterval);elapsedInterval=restInterval=null}function closeWorkoutMenu(){const menu=document.querySelector('[data-workout-menu]'),toggle=document.querySelector('[data-workout-menu-toggle]');if(menu)menu.hidden=true;if(toggle)toggle.setAttribute('aria-expanded','false')}function paintRest(id,endAt){const shell=document.querySelector('#restTimer'),value=document.querySelector('#restTimerValue');if(!shell||!value)return;const update=()=>{const r=Math.max(0,Math.ceil((endAt-Date.now())/1000));value.textContent=`${String(Math.floor(r/60)).padStart(2,'0')}:${String(r%60).padStart(2,'0')}`;const ring=shell.querySelector('.live-rest-ring');if(ring)ring.style.opacity=r?'1':'.55';if(!r){clearInterval(restInterval);restInterval=null;localStorage.removeItem(restKey(id));shell.classList.add('is-finished')}};update();if(endAt>Date.now())restInterval=setInterval(update,250)}function hydrateActiveScreen(){const page=document.querySelector('.active-workout-page');if(!page)return;closeWorkoutMenu();const id=page.dataset.activeSession,started=new Date(page.dataset.startedAt).getTime();localStorage.setItem(ACTIVE_KEY,id||'active');const elapsed=document.querySelector('#liveWorkoutElapsed');if(elapsed&&Number.isFinite(started)){const paint=()=>elapsed.textContent=formatElapsed(Date.now()-started);paint();elapsedInterval=setInterval(paint,1000)}if(id){const saved=Number(localStorage.getItem(restKey(id))||0);if(saved>Date.now())paintRest(id,saved);else if(saved)localStorage.removeItem(restKey(id))}}function rehydrate(){stopTimers();hydrateActiveScreen()}function watchForSessionEnd(sessionId){const started=Date.now(),timer=setInterval(()=>{const page=document.querySelector('.active-workout-page');if(!page&&location.hash.includes('/workouts')){localStorage.removeItem(ACTIVE_KEY);if(sessionId)localStorage.removeItem(restKey(sessionId));clearInterval(timer);return}if(Date.now()-started>12000)clearInterval(timer)},180)}if(localStorage.getItem(ACTIVE_KEY)&&!location.hash.includes('/workouts'))history.replaceState(null,'','#/workouts');new MutationObserver(()=>{clearTimeout(window.__ironlogActiveHydrate);window.__ironlogActiveHydrate=setTimeout(rehydrate,20)}).observe(document.documentElement,{childList:true,subtree:true});addEventListener('pageshow',rehydrate);document.addEventListener('visibilitychange',()=>{if(!document.hidden)rehydrate()});document.addEventListener('keydown',event=>{if(event.key==='Escape')closeWorkoutMenu()});document.addEventListener('click',event=>{const toggle=event.target.closest('[data-workout-menu-toggle]');if(toggle){const menu=document.querySelector('[data-workout-menu]');if(!menu)return;const open=menu.hidden;menu.hidden=!open;toggle.setAttribute('aria-expanded',String(open));return}if(!event.target.closest('[data-workout-menu]'))closeWorkoutMenu();const step=event.target.closest('[data-step]');if(step){const input=document.getElementById(step.dataset.stepTarget);if(input){const next=Math.max(Number(input.min||0),Number(input.value||0)+Number(step.dataset.step||0)),max=input.max===''?Infinity:Number(input.max);input.value=String(Math.min(max,Math.round(next*100)/100));input.dispatchEvent(new Event('input',{bubbles:true}))}}if(event.target.closest('[data-minimize-workout]'))location.hash='#/home';if(event.target.closest('#skipRestTimer')){const p=document.querySelector('.active-workout-page');if(p?.dataset.activeSession)localStorage.removeItem(restKey(p.dataset.activeSession))}const ending=event.target.closest('[data-complete-session],[data-cancel-session]');if(ending){const p=document.querySelector('.active-workout-page');closeWorkoutMenu();watchForSessionEnd(p?.dataset.activeSession)}},true);document.addEventListener('input',event=>{if(!event.target.closest('.live-inline-fields'))return;clearTimeout(saveTimer);saveTimer=setTimeout(()=>{const form=event.target.closest('form');if(form)form.dataset.edited='true'},120)},true);document.addEventListener('submit',event=>{const form=event.target.closest('[data-set-form]');if(!form)return;const page=document.querySelector('.active-workout-page'),id=page?.dataset.activeSession,seconds=Number(form.dataset.restSeconds||0);if(id&&seconds>0)localStorage.setItem(restKey(id),String(Date.now()+seconds*1000))},true);rehydrate();